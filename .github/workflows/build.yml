name: Build IPA
on: [push]
jobs:
  build:
    runs-on: macos-14   # hoặc macos-latest
    env:
      THEOS: ${{ github.workspace }}/theos
      SDKROOT: /Applications/Xcode.app/Contents/Developer/Platforms/iPhoneOS.platform/Developer/SDKs/iPhoneOS.sdk
    steps:
      - uses: actions/checkout@v4

      - name: Install Theos
        run: |
          git clone --recursive https://github.com/theos/theos.git ${{ env.THEOS }}
          # Xcode toolchain đã có sẵn, không cần cài thêm

      - name: Build package
        run: |
          make package FINALPACKAGE=1

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: modded-ipa
          path: packages/*.ipa
